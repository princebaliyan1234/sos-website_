---
const navItems = [
  { href: '/', text: 'Home' },
  { href: '/blog', text: 'Blog' },
  { href: '/books', text: 'My Books' },
  { href: '/join', text: 'Join Us' },
];
---

<nav class="fixed w-full top-0 z-50 bg-gray-900/80 backdrop-blur-md border-b border-purple-500/20">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex justify-between h-16">
      <div class="flex items-center">
        <a href="/" class="text-xl font-bold text-white hover:text-purple-400 transition-colors">Silence of Scribes</a>
      </div>
      <div class="flex items-center">
        {navItems.map(item => (
          <a
            href={item.href}
            class="ml-8 text-gray-300 hover:text-purple-400 transition-colors"
          >
            {item.text}
          </a>
        ))}
      </div>
    </div>
  </div>
</nav>
<div class="h-16"></div> <!-- Spacer for fixed navbar -->